config_string:
- userhost
- uso:testorg1
- default
default_channels:
- main
- https://urlb.example.com/repo/main,https://urlb.example.com/repo/r
- main,https://repo.anaconda.com/pkgs/msys2
# Experiencing an apparent bug in conda-build that collapses
# the variants if I have a blank string in here instead of "blank"
# so we are fixing this here and in meta.yaml
channel_alias:
- https://urla.example.com/repo
- "blank"
- https://urlc.example.com
repo_token:
- token_a
- token_b
- token_c
custom_build:
- test_a
- test_b
- test_c
zip_keys:
- config_string
- default_channels
- channel_alias
- repo_token
- custom_build